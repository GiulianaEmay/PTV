# Token de acceso a la API de Loyverse
# Se genera en: Back Office de Loyverse > Configuracion > Integraciones > API access token
LOYVERSE_ACCESS_TOKEN=pega_aqui_tu_token

# Puerto local del servidor del dashboard
PORT=3000
